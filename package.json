{
  "name": "codeclouds-frontenddev-skill",
  "version": "1.1.1",
  "description": "Pixel-perfect webpage cloning skill for Claude Code",
  "scripts": {
    "setup":        "npm install",
    "requirements": "pip3 install -r requirements.txt",
    "capture":      "node scripts/capture.js",
    "extract":      "node scripts/extract.js",
    "build":        "node scripts/build.js"
  },
  "dependencies": {
    "fs-extra":   "^11.0.0",
    "puppeteer":  "^22.0.0"
  },
  "optionalDependencies": {
    "playwright": "^1.43.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
